// src/layouts/Layout.astro
---
interface Props {
  title: string;
  description?: string;
}

const { title, description = "Lifestyle Coach & Digital Marketing Tutor" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body class="min-h-screen flex flex-col">
    <slot name="header">
      <div class="sticky top-0 z-50 bg-white/90 backdrop-blur-sm border-b">
        <Navbar />
      </div>
    </slot>
    
    <main class="flex-grow">
      <slot />
    </main>

    <slot name="footer">
      <Footer />
    </slot>
  </body>
</html>

// src/components/global/Navbar.astro
---
const navItems = [
  { title: 'Home', path: '/' },
  { title: 'About', path: '/about' },
  { title: 'Services', path: '/services' },
  { title: 'Blog', path: '/blog' },
  { title: 'Contact', path: '/contact' }
];

const currentPath = Astro.url.pathname;
---

<nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  <div class="flex justify-between h-16">
    <div class="flex">
      <div class="flex-shrink-0 flex items-center">
        <a href="/" class="text-xl font-bold">
          Your Brand
        </a>
      </div>
      <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
        {navItems.map((item) => (
          <a
            href={item.path}
            class={`inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium ${
              currentPath === item.path
                ? 'border-indigo-500 text-gray-900'
                : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'
            }`}
          >
            {item.title}
          </a>
        ))}
      </div>
    </div>
    <div class="flex items-center">
      <a
        href="/contact"
        class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-indigo-600 hover:bg-indigo-700"
      >
        Book Consultation
      </a>
    </div>
  </div>
</nav>

// src/components/global/Footer.astro
---
const currentYear = new Date().getFullYear();
---

<footer class="bg-gray-50">
  <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <div class="col-span-1 md:col-span-2">
        <h3 class="text-xl font-bold">Your Brand</h3>
        <p class="mt-4 text-gray-600">
          Empowering individuals through lifestyle coaching and digital marketing education.
        </p>
      </div>
      <div>
        <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider">Services</h3>
        <ul class="mt-4 space-y-4">
          <li>
            <a href="/services/digital-marketing-tutoring" class="text-gray-600 hover:text-gray-900">
              Digital Marketing Tutoring
            </a>
          </li>
          <li>
            <a href="/services/lifestyle-coaching" class="text-gray-600 hover:text-gray-900">
              Lifestyle Coaching
            </a>
          </li>
        </ul>
      </div>
      <div>
        <h3 class="text-sm font-semibold text-gray-400 uppercase tracking-wider">Contact</h3>
        <ul class="mt-4 space-y-4">
          <li>
            <a href="/contact" class="text-gray-600 hover:text-gray-900">
              Get in Touch
            </a>
          </li>
          <li>
            <a href="/about" class="text-gray-600 hover:text-gray-900">
              About Me
            </a>
          </li>
        </ul>
      </div>
    </div>
    <div class="mt-8 border-t border-gray-200 pt-8">
      <p class="text-center text-gray-400">
        © {currentYear} Your Brand. All rights reserved.
      </p>
    </div>
  </div>
</footer>